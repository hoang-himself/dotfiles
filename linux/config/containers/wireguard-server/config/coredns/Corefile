# Plugin ordering is static
# See plugin.cfg

. {
  metadata
  cancel
  reload
  bufsize
  errors
  log
  any
  cache
  rewrite name regex (?:.+)+\.hoang-himself\.dev systemd-swag.dns.podman answer auto
  minimal
  loop
  forward dns.podman /etc/resolv.conf
  forward . 10.90.1.53 8.8.8.8 1.1.1.1 208.67.222.222 94.140.14.14 {
    policy sequential
  }
}
